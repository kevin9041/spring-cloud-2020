server:
  port: 3377

spring:
  application:
    name: service-nacos-config
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
#        group: group1
        group: group2
